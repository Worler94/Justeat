<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.luca.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Mainù">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivityRegistrazione"
            android:label="@string/title_activity_main_registrazione"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".SchermatadiBenvenuto">
            <intent-filter>
                <action android:name="android.intent.action.SEND"></action>
                <action android:name="android.intent.action.SENDTO"></action>
                <category android:name="android.intent.category.DEFAULT"></category>
                <data android:scheme="mail"></data>
                <data android:scheme="mailto"></data>
            </intent-filter>
        </activity>
    </application>

</manifest>